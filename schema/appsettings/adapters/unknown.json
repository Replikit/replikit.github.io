{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "examples": [
    {
      "Type": "<type>",
      "Token": "<token>"
    }
  ],
  "properties": {
    "Type": {
      "type": "string",
      "description": "Adapter type",
      "not": {
        "enum": ["tg"]
      }
    },
    "Token": {
      "description": "Platform access token",
      "type": "string"
    }
  },
  "required": ["Type"]
}
